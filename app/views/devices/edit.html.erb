<% content_for(:title, "Edit Device") %>

<%= render :partial => "devices/form", :locals => {:form_action => url_for(:controller => :devices, :action => :update, :id => @device.id), :submit_text => "Update"} %>
